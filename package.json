{
  "name": "hackspace-utils",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:spaceman": "dotenv -- lerna run --scope @teessidehackspace/spaceman start:dev --stream",
    "start:graphql": "dotenv -- lerna run --scope @teessidehackspace/gocardless-graphql start --stream",
    "start:webhook": "dotenv -- lerna run --scope @teessidehackspace/gocardless-webhook start --stream"
  },
  "devDependencies": {
    "@types/node": "^14.11.1",
    "cross-env": "^7.0.2",
    "dotenv-cli": "^4.0.0",
    "esm": "^3.2.25",
    "lerna": "^3.22.1",
    "ts-node-dev": "^1.0.0",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.0.3"
  }
}
